# Codex Fast

> Enable Fast mode and related speed options in the Codex app.

Codex Fast is a launcher for **Windows x64** and **Apple silicon Macs**. It dynamically patches the app's renderer resources at startup without modifying its files on disk.

The patches remain active only while the app is running.
